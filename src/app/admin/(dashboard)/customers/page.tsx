import { ComingSoon } from "@/features/dashboard/components/coming-soon";

export default function CustomersPage() {
  return (
    <ComingSoon
      title="Customers"
      description="Customer profiles, booking history, lifetime value and CSV export are landing in the next increment."
    />
  );
}
